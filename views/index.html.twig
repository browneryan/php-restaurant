<!DOCTYPE html>
<html>
  <head>
    {% include '/includes/head.html.twig' %}
  </head>
  <body>
    <div class="container">
        <h1>Welcome to Best Restaurants</h1>
        <hr>
        <div class="row">
          <div class="col-md-6">
            <p>Create a new cuisine type:</p>
            <form action="/cuisines" method="post">
                <label for="name">Cuisine type:</label>
                <input id="name" name="name" type="text">

                <button class = "btn btn-success" type="submit">Add Cuisine</button>
            </form>
          </div>
          <div class="col-md-6">
            {% if cuisines is not empty %}
                <p>Cuisine Types:</p>
                <ul>
                    {% for cuisine in cuisines %}
                        <li><a href="/cuisines/{{ cuisine.getCuisineID }}">{{ cuisine.getName }}</a></li>
                    {% endfor %}
                </ul>
            {% else %}
              <p>There are no cuisine types yet!</p>
            {% endif %}
            <form action="/delete_cuisines" method="post">
              <button class="btn btn-danger" type="submit" name="button">Nuke everything</button>
            </form>

          </div>
        </div><!--row-->
      </div><!--container-->
  </body>
</html>
